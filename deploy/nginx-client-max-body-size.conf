# Snippet for aws-api.reparv.in (Ubuntu nginx).
# Default client_max_body_size is 1m — blog/news multipart uploads (HTML + image) exceed that.
#
# Add inside the server { } block for aws-api.reparv.in, then:
#   sudo nginx -t && sudo systemctl reload nginx

client_max_body_size 20M;
